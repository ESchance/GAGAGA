# 嘎宇宙项目规则

## 项目概述

这是一个基于 React + Supabase 的多人实时在线交流论坛，包含世界观系统和入场动画。

## 技术栈

- 前端: React 18 + Vite + Tailwind CSS
- 后端: Supabase (PostgreSQL + Realtime + Auth)
- 部署: Cloudflare Pages

## 代码规范

1. 使用函数式组件和 Hooks
2. 使用 Tailwind CSS 进行样式设计
3. 使用 Supabase 进行数据操作
4. 所有组件使用 React.memo 优化性能
5. 使用 useCallback 和 useMemo 优化性能

## 文件结构

- `src/components/` - 可复用组件
- `src/pages/` - 页面组件
- `src/lib/` - 工具函数
- `src/hooks/` - 自定义 Hook
- `src/animation/` - 入场动画

## 数据库

- 使用 Supabase PostgreSQL
- 所有表都启用 RLS
- 使用外键关联数据

## 安全注意事项

1. 环境变量不上传到 GitHub
2. 只有 user01 是超级管理员
3. 删除用户需要先删除关联数据
4. RLS 策略保护所有表

## 开发命令

```bash
npm install      # 安装依赖
npm run dev      # 启动开发服务器
npm run build    # 构建生产版本
```
